export interface HealthData {
    uptime?: string;
    status?: string;
    version?: string;
    update_available?: boolean;
    current_user?: any;
    debug?: boolean;
    setup_required?: boolean;
}
